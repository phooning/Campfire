import React, { ReactNode } from 'react';
import { View, Text } from 'react-native';

const App = (): ReactNode => {
  return (
    <View style={{}}>
      <Text>Hello world</Text>
    </View>
  );
};

export default App;
