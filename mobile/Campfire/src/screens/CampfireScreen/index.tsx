import React, {ReactElement} from 'react';
import {View} from "react-native";
import {useWebSocket} from "utils/sockets";

const CampfireScreen = (): ReactElement => {
  // const socket = useWebSocket(wsUri);
  return (
    <View>

    </View>
  );
}

export default CampfireScreen;
