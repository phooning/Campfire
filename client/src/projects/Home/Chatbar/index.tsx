import React, { ReactElement } from "react";
import styled from "styled-components";

const ChatbarWrapper = styled.div`
  padding: 25px 35px 60px;
`;

const Chatbar = (): ReactElement => {
  return (
    <ChatbarWrapper>

    </ChatbarWrapper>
  )
};

export default Chatbar;
